# 화면 인터페이스 구성 초안

- 버전: v0.2 / 2026-09-16 — 설명 본문의 구체성 보완
- 상태: 화면 배치와 사용 흐름을 논의하기 위한 초안. 구현 완료 또는 공개 배포를 뜻하지 않는다.
- 관련 문서: [구현 명세서](IMPLEMENTATION_SPEC.md), [공통 진행 지침](AGENTS.md).
- 임시 화면 이름: `코드 회고`. 서비스 이름은 미정이다.

## 1. 확정한 방향과 제안 구분

**사용자가 선택한 방향:** 어두운 개발 도구형. 코드 편집기와 비슷한 분위기로 구성한다.

**사용자가 추가한 설명 기준:** 상속을 통한 확장 방식, Machine과 SO의 관계, SO에 담긴 기계 종류·티어·채집 설정처럼 구체적인 구현을 문단으로 풀어쓴다. 글자 크기보다 설명 내용의 길이와 깊이를 보완하는 요구다.

**이번 초안의 제안:** 데스크톱 우선, 시스템 설명 중심의 3열 작업 화면, 별도의 초기 설정 단계, 검토한 설명을 포트폴리오 초안으로 모으는 흐름. 아래의 화면 폭·색·문구·반응형 기준은 변경 가능한 제안이다.

GitHub 아이디를 직접 입력하고 변경할 수 있어야 한다. `gominsu08`은 샘플 값이며 실제 서비스의 고정 계정이 아니다. 네임스페이스나 폴더 이름으로 담당자를 확정하지 않는다.

## 2. 전체 화면 흐름

`저장소 연결 → 분석 범위·담당 확인 → 시스템 해설 작업 → 포트폴리오 정리`

| 화면 | 사용자가 결정하거나 수행할 일 | 대표 행동 |
| --- | --- | --- |
| 저장소 연결 | GitHub URL, 본인 아이디, 개인·팀 프로젝트 선택 | 저장소 확인 |
| 범위·담당 확인 | 포함할 코드, 외부 코드 제외, 담당 범위 보정 | 선택한 코드 분석 |
| 시스템 해설 | 시스템별 설명 읽기, 근거 확인, 코멘트 저장·반영, 버전 검토 | 선택한 코멘트 반영 / 검토 완료 |
| 포트폴리오 정리 | 포함할 시스템 선택, 담당·의도·성과 확인 | 초안 미리보기 / 내보내기 |

실제 서비스에서는 분석 시작과 해설 화면 사이에 진행 상태를 표시한다. 단계·처리량·부분 실패·재시도·취소를 보여주며, 근거 없는 예상 완료 시간이나 진행률은 만들지 않는다. 이번 시안은 완료된 예시 화면으로 바로 이동한다.

## 3. 저장소 연결

- 왼쪽: 도구의 용도와 짧은 사용 흐름. 홍보용 대시보드나 가상의 통계는 넣지 않는다.
- 오른쪽: URL, GitHub 아이디, 프로젝트 유형을 한 폼에서 입력한다.
- 저장소 확인이 끝나면 기본 브랜치를 보여주고, 다른 브랜치·참조 선택은 고급 설정으로 제공한다.
- 형식 오류·접근 불가·비공개 저장소·요청 제한은 해당 입력 또는 단계 가까이 표시한다.
- 아이디 입력은 기여 분석 기준이다. 인증이나 계정 소유 증명을 대신하지 않는다.

## 4. 분석 범위와 담당 확인

- 코드 묶음별로 포함 체크, 경로, 분류 근거, 담당 범위를 보여준다.
- 담당 값은 `미확인 / 내 담당 / 공동 담당 / 다른 참여자`를 기본안으로 한다. 실제 참여자 선택과 여러 계정 연결은 후속 상세 설계에서 구체화한다.
- 개인 프로젝트에서는 참여자 구분 항목을 줄이고, 프로젝트 코드와 외부·생성 코드 구분은 유지한다.
- 팀 프로젝트에서 이름·네임스페이스가 비슷하다는 이유로 `내 담당`을 자동 확정하지 않는다.
- 샘플의 GMS·PSW 경로는 분류 예시다. 두 경로를 두 사람 또는 특정 계정의 소유로 단정하지 않는다.
- 담당 범위 수정 후 기존 검토 완료 표시와 결과 유효성을 다시 확인하도록 한다. 구현 명세서의 설정 버전·분석 버전 규칙을 따른다.

## 5. 핵심 작업 화면: 3열 구성

### 왼쪽 — 시스템 탐색

- 폴더 구조보다 기능·시스템 목록을 먼저 보여준다.
- 시스템 선택 시 가운데 설명과 오른쪽 코멘트가 함께 바뀐다.
- 분석 설정으로 돌아가는 행동과 현재 기여 분석 기준 계정을 표시한다.
- 후속 설계에서 시스템 검색, 분류 수정, 이름 변경, 병합·분리 등의 배치를 구체화한다.

### 가운데 — 설명과 근거

- 시스템 이름, 설명 버전, 검토 상태, 담당 범위를 상단에 둔다.
- 탭은 `시스템 설명 / 근거 코드 / 수정 이력`을 제안한다.
- 설명 순서: 짧은 도입 요약 → 설계 선택과 구현 방식을 풀어쓴 본문 → 동작·구성 관계 → 사용자 확인이 필요한 부분.
- 본문은 하나의 핵심 구현 주제를 2~3개 문단으로 설명하는 수준에서 시작한다. 실제 클래스·데이터가 어떻게 연결되며 어떤 변경·확장이 가능한지 담고, 내용에 따라 분량을 조절한다. 본문을 한 줄 요약이나 작은 카드 여러 개로 대체하지 않는다.
- 사용자의 기계 시스템 예시를 시안의 첫 화면으로 보여준다. 사용자 제공 내용을 바탕으로 문체를 다듬은 것으로 표시하고, 해당 설계 내용의 코드 대조는 별도로 남긴다.
- 긴 설명도 문단 간 여백을 두고 자연스럽게 이어 읽을 수 있게 한다. 도입 요약을 접거나 펼쳐야만 본문에 접근하는 구조는 피한다.
- 설명 문단마다 코멘트 진입점과 근거 파일·심볼을 연결한다.
- 코드 근거·사용자 진술·AI 추정을 이름으로 구분한다. 색만으로 의미를 전달하지 않는다.
- 근거는 스냅샷 커밋의 파일·줄 위치를 열 수 있어야 한다. 이번 시안은 대표 파일과 심볼만 보여주며 줄 단위 뷰는 다음 단계에서 설계한다.
- 코드 전체를 항상 펼치기보다는 선택한 설명과 관계 있는 부분부터 보여준다.
- `검토 완료`를 별도 행동으로 제공한다. 새 수정안이 만들어지면 다시 검토해야 한다.

### 오른쪽 — 코멘트와 설명 보완

- 코멘트가 연결된 시스템·문단과 짧은 인용문을 보여준다.
- 실제 담당, 개발 의도, 강조할 내용, 잘못 설명된 부분을 자유롭게 적을 수 있다.
- **코멘트 저장:** 기록을 남긴다. 이 단계에서는 설명을 변경하지 않는다.
- **선택한 코멘트 반영:** 선택한 기록으로 새 설명 버전을 만든다. 이전 버전은 보존한다.
- 반영 대기·반영 완료를 구분하고, 재생성 결과와 이전 설명을 비교할 수 있게 한다.
- 시스템을 옮겨도 작성 중인 코멘트를 잃지 않도록 한다.
- 오른쪽 패널이 답답한 경우 본문 아래에 코멘트를 배치하는 대안도 비교한다. 최종 위치는 미확정이다.

## 6. 포트폴리오 정리

- 왼쪽에는 포함할 시스템과 검토 상태, 오른쪽에는 문서 미리보기를 둔다.
- 검토 완료한 시스템을 기본 선택한다. 미검토 내용을 포함하면 상태를 명확히 표시한다.
- 제안 문서 구조: 프로젝트 개요 → 담당 역할 → 시스템 구현 → 사용자에게 확인된 개발 의도 → 확인할 항목.
- 시스템 구현에는 상세 본문을 포함하여 상속·클래스·데이터 설정·확장 방식의 설명이 유지되도록 한다. 화면의 도입 요약만 복사하지 않는다.
- 공동 기여를 개인 기여로 바꾸지 않으며, 성능 수치·성과·당시의 개발 순서를 지어내지 않는다.
- Markdown 다운로드와 복사를 우선 후보로 둔다. 최종 내보내기 형식은 추가 논의한다.
- 이번 시안의 `텍스트 선택`은 예시 출력 구조 확인용이다. 실제 AI 포트폴리오 생성 기능은 아니다.

## 7. 시각 스타일과 화면 크기

| 항목 | 현재 제안 |
| --- | --- |
| 분위기 | 어두운 편집기형, 얇은 구분선, 작은 모서리 곡률 |
| 기본 배경 | `#11151B` |
| 패널 배경 | `#181D25` |
| 강조색 | 차분한 블루 `#A8C6FF`, 그린·퍼플 대안 비교 가능 |
| 본문 | 한국어 산세리프, 14~18px 비교. Figma 확인 글꼴은 IBM Plex Sans KR |
| 코드 | JetBrains Mono 또는 시스템 고정폭 대체 글꼴 |
| 3열 배치 | 탐색 약 190~248px / 유동 본문 / 코멘트 약 272~368px |
| 좁은 화면 | 코멘트를 본문 아래로 이동하고, 더 좁으면 시스템 선택을 상단으로 이동 |

본문이 읽기 어려울 만큼 화면에 정보를 밀어 넣지 않는다. 중요한 상태를 색만으로 표현하지 않으며, 버튼·입력은 키보드로 조작 가능해야 한다. 모바일 세부 탭과 패널 전환은 후속 디자인에서 확정한다.

## 8. 시안 산출물과 현재 제한

- [Figma 파일](https://www.figma.com/design/6AoL0Ie2Le89wIdq6KlPW1): 새 파일, 시작·작업 화면 프레임과 작업 화면의 구조만 생성했다. **연결 계정의 Starter 플랜 MCP 호출 한도 때문에 내용 배치가 중단된 미완성 파일**이다. 완성된 Figma 디자인으로 취급하지 않는다.
- 대화 내 클릭 시안 경로: `C:/Users/daon_/.codex/visualizations/2026/09/16/01a0aa7b-4760-77f0-b3c6-be04a0f1336e/code-retrospective-ui.html`.
- 클릭 시안은 화면 이동, 시스템 선택, 입력 편집, 담당 지정, 코멘트 저장·반영의 구분, 검토 완료, 문서 구조 미리보기를 제공한다.
- 클릭 시안은 외부 API를 호출하지 않는다. 샘플 저장소 설명을 사용하며 URL·범위를 바꿔도 새 코드를 분석하지 않는다. 코멘트 반영 시 AI 재작성 대신 사용자 문구를 예시 위치에 그대로 표시한다.
- 기계 시스템의 상세 설명은 사용자가 제공한 예시를 바탕으로 작성했다. 관련 코드 후보와 검증된 문단 근거를 구별하며, 코드 검증 결과인 것처럼 표시하지 않는다.
- 시안의 변경 상태는 메모리에만 있으며 새로고침하면 초기화된다. 이는 최종 서비스의 저장 정책을 의미하지 않는다.
- 실제 서비스는 공개 URL에서 AI 기능이 동작해야 한다는 요구를 그대로 유지한다. 이번 작업은 UI 논의용이며 배포는 수행하지 않았다.

## 9. 다음에 함께 정할 사항

1. 3열을 유지할지, 코멘트를 본문 아래에 둘지.
2. 설명 중심으로 시작할지, 설명·코드를 동시에 보는 구성을 더 강조할지.
3. 시스템 분류 수정과 담당자 수정 화면의 상세 조작 방식.
4. 수정 전후 비교, 분석 진행·실패·빈 결과 화면.
5. 포트폴리오 문서의 실제 목차와 내보내기 형식.
6. 서비스 이름, 강조색, 본문 밀도.

위 사항을 합의한 뒤 구현 명세서에 반영한다. 현재 초안의 모든 세부 배치를 확정 요구사항으로 간주하지 않는다.
